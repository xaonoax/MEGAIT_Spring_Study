<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 연결할 인터페이스 경로를 namespace에 작성 -->
<mapper namespace="com.megait.example.mappers.TransactionMapper">
	<insert id="insertTest1">
		INSERT INTO TBL_TEST1(ID, NAME)
		VALUES (#{id}, #{name})
	</insert>
	<insert id="insertTest2">
		INSERT INTO TBL_TEST2(NUM, NAME)
		VALUES (NEXTVAL(SEQ_TEST)), #{NAME})
	</insert>
</mapper>